import React from 'react';
import PlaceholderPage from '../../components/PlaceholderPage/PlaceholderPage';

/**
 * Grades & GPA Dashboard page – T023
 * Displays the student's grades per course, GPA, and academic progress.
 * Full implementation: see T022 (grade retrieval service).
 */
export default function GradesPage() {
  return (
    <PlaceholderPage
      icon="📊"
      title="Grades & GPA"
      description="Your grade summary, per-course results, and cumulative GPA chart will be displayed here. (T023)"
    />
  );
}
