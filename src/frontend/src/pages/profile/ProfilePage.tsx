import React from 'react';
import PlaceholderPage from '../../components/PlaceholderPage/PlaceholderPage';

/**
 * Student Profile page – T017
 * Displays and allows editing of the authenticated student's profile.
 * Full implementation: see T015 (backend endpoint) and T016 (profile service).
 */
export default function ProfilePage() {
  return (
    <PlaceholderPage
      icon="👤"
      title="My Profile"
      description="View and update your personal details, contact information, and academic preferences. (T017)"
    />
  );
}
